# Playable Builder Kit — Claude

**Upload to Project knowledge:** `ai/CLAUDE-PACK.md` (one file, all rules).

**Project instructions:**

```text
Read ai/CLAUDE-PACK.md before every playable task.
"install" / setup → ask permission, run bash scripts/setup-env.sh --yes (Node >=20.19, pnpm 9).
"Create new playable" → ask chủ đề, auto-match template, run pnpm playable:new.
Deliver single HTML via studio:export / compose:build / build:single.
```

Cursor / full docs: `ai/README.md` → `ai/INSTRUCTIONS.md`.
